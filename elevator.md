# Elevator

*"Delta-encoded context: a deterministic state ledger where byte-matched unchanged history is carried as precomputed state the model trusts implicitly, and only the diff plus new input pays fresh inference -- active-inference economics for context windows."*

