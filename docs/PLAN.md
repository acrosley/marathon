# Marathon — Execution Plan

**Status:** Phase 1 exit criterion met 2026-08-18 (position-shifted KV reuse in vLLM: edit-turn prefill 6.1× faster, output byte-identical); multi-span/moved-block reuse and a 144-session distribution eval done 2026-08-19; see findings.md, consolidated in [phase1-report.md](phase1-report.md) · **Updated:** 2026-08-19 · **Companion:** [DESIGN.md](../DESIGN.md) (doc 0001), [protocol.md](protocol.md), [findings.md](findings.md), [phase3-design.md](phase3-design.md)

This plan sequences the delta-encoded context architecture from pure systems work (buildable today against existing APIs) toward the research bet (training the trust contract). Each phase has explicit exit criteria; a phase is not done until its metrics are collected and its correctness gate passes.

## Phase 0 — Deterministic core and prefix-cache maximization *(done)*

Build the ledger, diff engine, and turn protocol; use them to canonicalize state so unchanged history always serializes byte-identically and append-only, making provider prefix caching hit maximally. No custom model, no self-hosted inference.

Delivered so far: canonical serialization with the append-only prefix guarantee, hash-chained ledger with tamper detection, rsync-style block delta engine with randomized round-trip property tests, turn protocol with integrity verification (reconstruction is proven against the target hash, never assumed), offline benchmark harness, experimental live TTFT/cache probe, CI (lint + tests on 3.10 and 3.12).

Done 2026-08-18: live probe run against the Anthropic API (cache-read = full history on unchanged-prefix turns; one early edit collapses it — see [findings.md](findings.md)); session runner (`session.py`) with the canonical serializer as the single path to the wire; full-context replay correctness gate in CI (`tests/test_replay_gate.py`); baseline metrics published in README and findings.md. Still open: TTFT flat-vs-growing needs longer sessions than the probe has run; single-writer concurrency and store eviction are tracked under cross-cutting.

**Exit criteria (met 2026-08-18):** wire bytes per turn ~O(|diff| + |input|) in the offline bench; live probe shows cache-read tokens ≈ total history tokens on unchanged-prefix turns; correctness replay gate green in CI.

## Phase 1 — Self-hosted inference and warm-tier KV reuse *(current)*

Started 2026-08-18. Stack: WSL2 Ubuntu on the RTX 5090, `~/marathon-venv` with torch 2.13+cu130, vLLM 0.27.1, LMCache 0.5.3, model `Qwen/Qwen3-14B-FP8` (dense full-attention on purpose — hybrid Gated-DeltaNet models like Qwen3.5/3.8 carry recurrent state with no per-token KV on most layers, so non-prefix KV reuse can't be shown on them; that degradation is a Phase 1.5 question). Reproducible via `scripts/phase1_setup.sh`; probes via `scripts/phase1_probe.sh` → `marathon.local_probe` with modes `none` / `prefix` / `blend`. Local prefix-cache baseline measured (edit-turn collapse: 0.12 s → 1.29 s prefill), CacheBlend now runs fully native, and the route is closed: on the edit turn it ties full recompute at every recompute ratio (0.15/0.05/0.02), it stays 4–5× behind prefix caching on unchanged turns, and blend + prefix caching together crashes inside LMCache. Marathon's own position-shifted KV reuse is the route that works: `kvshift.py` (HF prototype) showed re-rotating cached keys by the edit's token offset δ (RoPE is a rotation, so the shift is exact) and recomputing only the edited span + new query keeps KL vs full recompute ≈0.002 with all planted facts exact at 0.7–9% of tokens forwarded; `vllm_shift_connector.py` (`--mode shift`) puts that inside vLLM 0.27 as a KV connector — on Qwen3-14B-FP8 the edit turn drops from 1.465 s to 0.242 s (6.1×), unchanged turns keep the 0.13 s prefix-cache path, and every turn's output is byte-identical to prefix mode. Since confirmed (findings.md, 2026-08-18/19): mid-history edit 4.7×, +186-token grow edit 5.9×; multi-span edits cost +66 ms per edited message in vLLM (k=1 6.3×, k=2 4.5×, k=4 3.0×, k=8 1.9×; break-even ≈17 edited messages), moved blocks are recomputed rather than transplanted (transplanting KV that summarised a different prefix produces garbage; a pure move still gets 4.0×); a 144-session Qwen3-8B eval (5 edit kinds, 7 query types) shows re-rotated reuse forwards 1.5–1.6% of tokens for median KL 0.003 vs prefix caching's 66–68% — the only failure class is edits to *governing* spans (system prompt / standing instructions; ~9× KL, independent of position or downstream size), which `reuse_plan` flags for repair. North-star measured 2026-08-19: edit-turn TTFT 0.15→0.26 s from 4k to 30k of history while prefix caching goes 0.42→3.94 s (15.1× at 30k), with the shifted copy on a fused Triton kernel at 1.43 TB/s; connector is session-keyed with LRU eviction and two-session isolation verified. Open: TP/preemption/chunked-prefill interactions untested; Phase 2.

## Phase 1.5 — Hybrid (linear-attention) models *(answered 2026-08-19, HF prototype)*

Measured on `Qwen/Qwen3.5-4B` (24 Gated-DeltaNet layers / 8 full attention, partial rotary + mRoPE) via `kvshift_hybrid.py` and `scripts/kvshift_hybrid.sh`. Re-rotation is still exact on the 8 attention layers (max abs error 1e-5 through the model's own rotary module, rotating only the 64 rotary dims). The other 24 layers have no per-token KV, so reuse becomes replay: cache each linear layer's own post-conv `(q, k, v, beta, g)` for the unchanged suffix — its true KV analogue — and roll only the recurrence, at 1.39% of a token-forward per replayed token. Result: the edit turn costs **1.5–2.2% of full-recompute FLOPs** at klmean 0.0017–0.0020 on planted facts, i.e. the dense win transfers on the compute axis — but it needs **~18x the cache bytes per token** (566 KB vs 32 KB), a fused scan kernel before wall clock follows the FLOPs (this prototype only gets 1.2–1.6x, on transformers' pure-torch fp32 chunk scan), and a new repair: an edit far from the end of a long context gets washed out of the decaying recurrent state, and recomputing the first ~256 tokens of the suffix fixes it. Reusing only the end-of-context state (what a serving system does by default) is 5–25x worse in KL. See findings.md.

**Open:** no distribution eval (three scenarios, single runs); M in first-M uncalibrated; nothing of this is in a serving engine.

Original plan text: stand up vLLM with LMCache to move beyond the prefix restriction: store KV for reused text segments regardless of position and recombine with selective recomputation (CacheBlend-style blending). The delta wire format from Phase 0 becomes the input that decides which KV segments are reusable.

**Exit criteria (met 2026-08-18, see findings.md):** measured TTFT improvement on mid-edit sessions (where prefix caching alone fails) versus the Phase 0 baseline — 6.1× on the edit turn; correctness replay gate still green and output parity byte-identical; memory budget for hot KV: 164 KB/token on Qwen3-14B (40 layers × 8 KV heads × 128 × 2 × bf16), 2.7 GB for the 16k-token store buffer.

## Phase 2 — Cold tier and recall-on-miss *(current, started 2026-08-19)*

Add the lossy tier: deep history demoted to embeddings/summaries with a paging policy, and recall-on-miss promotion back into the active window when a diff or query touches demoted content. The ledger's content addressing makes promotion exact — cold content is a pointer to verifiable bytes, not a paraphrase.

Delivered 2026-08-19: `marathon.cold` — a paging policy over a `Session` that keeps the model-facing active view under an `--active-window N` token budget by demoting the oldest non-governing messages (never the system prompt, never a `governing` span, never the last K turns) to a deterministic stub line carrying the message's own content address, `[cold #12 3f9a1c04: <first ~12 words>]`. The view is a pure function of `(messages, demoted, evicted)`, so hashes, replay and the reuse plan stay exact — and because only *content* changes, a demotion is an ordinary shrink edit and a promotion an ordinary grow edit, which is exactly the shape position-shifted KV reuse already prices cheaply. Stubs alone are still O(n), so the oldest stubs are *evicted* from the view once the budget is blown; the bytes stay in the ledger and the retriever still searches them, which is what makes the window genuinely bounded rather than merely 20x smaller. Recall-on-miss has both triggers: **exact** (the turn's delta touches a demoted message, detected by canonical byte comparison) and **query** (mean-pooled `all-MiniLM-L6-v2` on CPU, top-k≤2 above a cosine threshold), with every promotion and demotion logged with a reason. Wired into `MarathonServer` so paging flows through the same verified-state + reuse-plan + shift-connector path; `marathon.cold_eval` + `scripts/cold_eval.sh` run the eval. See [protocol.md](protocol.md) "Cold tier" and findings.md.

Status 2026-08-19, against the three exit criteria (Qwen3-14B-FP8, 20 sessions x 70 turns, window 8192, history p50 25k):
**(1) Bounded window — met.** Median active tokens go flat from turn 30 (7916 -> 7655 as the history doubles, max 8075 against an 8192 budget) while the full-context reference grows 3755 -> 25691 -> 35674. Holds identically with the connector on or off.
**(2) Recall-on-miss restores demoted content — met.** 102 of 120 fact questions had their answer already paged out; the query trigger promoted the right message for 91.2% of them. It is the whole ball game: the same policy scores exact-match 0.008 without recall and 0.817 with it. Retrieval had to be scored per 60-word chunk rather than per message (whole-message pooling recalled 39%).
**(3) Quality delta vs full-context replay — partial, and still partial.** 0.817 against a 0.942 reference is -12.5 points; that is a large gain over the naive baseline and not within any tolerance worth the name. Unchanged by the re-measure.

Composition with Phase 1 re-measured 2026-08-19 and **not yet a win**. Turns actually served from re-rotated KV cost 0.207 s against 0.537 s for prefix caching on the same paged workload (2.6x), and unbounded reuse runs the whole condition 3.1x faster — the mechanism composes. But quality tracks the staleness refresh rate (exact-match 0.111 unbounded, 0.444 at either bounded ceiling, 0.889 connector-off on the matched subset), and the refresh turn itself currently costs 48 s against 0.537 s for the same prefill without the connector (isolated: 16.7 s vs 731.6 s for max_stale 999 vs 1 on an identical config), which is a serving bug on the post-reuse save path rather than a policy cost. So the cold tier today serves best with the connector *off*. A churn-based ceiling (`--max-churn`, refresh when the tokens rewritten in front of the reused span exceed a fraction of it) is implemented and is strictly better than counting turns — same exact-match as `max_stale=1` for 2.25x less mean prefill — but that is a choice between two configurations that both lose to connector-off. Also fixed: `moved` was classifying an order-preserving line deletion as a relocation, which cost an eviction turn all of its reuse (worth 43% of reusable turns at a 4096 window, 84% at 2048).

Composition re-measured 2026-08-20 with the two measurement bugs fixed (scored turns asked on two consecutive turns so they cannot phase-lock onto the reuse/refresh alternation, and the store pre-sized from the active window so no save has to grow a buffer at `gpu_memory_utilization=0.93`). The answer is **no, and now for a reason that is not a serving bug**. On the paged turns of a 40-turn 14B run at window 8192, connector-off scores exact-match 14/14 at 0.725 s mean; `--max-churn 1.0` is 1.27x faster at **4/14**; `--max-churn 0.25` is **4x slower** at 7/14. Splitting by turn kind is the result that matters: turns served from re-rotated KV run at 0.25-0.27 s (2.7x faster than connector-off) and answer correctly **2/8 and 1/11**, while refresh turns answer 5/6 and 3/3 — matching connector-off — and cost 2.0-5.2 s, several times the full prefill they are equivalent to. The churn ceiling works as a dial (reuse fraction 32% -> 55%, exact-match 0.50 -> 0.29 with it) but has no setting where both axes are acceptable, because reuse is wrong at every churn level reached, including turns held under a 0.25 churn fraction. Yesterday's claim that cumulative cold count predicts a wrong turn does not survive re-checking — no available metric separates hit from miss, and 2 hits in 7 is consistent with reuse simply being unreliable on this workload. The refresh-turn cost is measured but still undiagnosed: it is not the store (zero evictions, zero refusals, loads at 229 GB/s) and it is not the save volume (one pass per turn), and the one hypothesis tested on hardware — the HND advanced-indexing gather in the save path — came back unchanged. **Recommendation stands: run the cold tier with the connector off.** Making the composition a win needs the reuse turns to become *correct*, which is a Phase 3 (stitched-cache robustness) problem, not a paging or serving one.

**Exit criteria:** bounded active-window size on unbounded sessions; recall-on-miss correctly restores demoted content in an eval of targeted questions about old context; quantified quality delta versus full-context replay stays within the agreed tolerance.

## Phase 3 — Robustness to stitched caches (research bet) *(2026-08-20: the adapter works on synthetic edits and transfers nothing to the paged workload; stale attention is necessary but not sufficient for serving's wrongness)*

**The original bet is largely superseded by Phase 1.** Phase 3 was written as "fine-tune the model to consume delta-formatted *text* and trust that absence-from-diff means unchanged". Position-shifted KV reuse gets that outcome without training and more completely: the baseline is never retransmitted, re-tokenized or re-prefilled, but the model's attention still runs over the full history, so its input distribution never changes and there is nothing to persuade it of. Measured, not asserted — 1.5–1.6% of tokens forwarded for median KL 0.003 and planted-fact retrieval 105/111 vs full recompute's 106/111 over 144 sessions. A text-level delta format would have been strictly worse on both axes.

**What remains is the harder form of the same idea**, and Phase 1 handed us a sharp instance. (a) The one residual failure class: an edit to a **governing** span (system prompt, standing instruction, persona, output format) leaves the reused suffix `S` still conditioned on the instruction that is no longer there — ~9× KL, the only class with items over KL 0.2, independent of edit position and of |S| (the 2×2 at seed 1235 settles it: flipping the flag moves KL 9×, moving the edit moves it by nothing). Selective recompute does not fix it: `first-32` cuts dep-instruction first-token KL 0.3492 → 0.0384 and `first-512` (10% of `S`) → 0.0177 while restoring agreement in only half the cases, and `blend-r0.30` is *worse* than reuse-all because its layer-1 K-deviation selector barely registers an instruction flip. The contamination is diffuse across the suffix, not a prefix of it. (b) The general case: the same shape recurs in relocated blocks (a δ = −10,153 transplant produces garbage; `--repair-first 256` does not help, so relocations are recomputed), in large δ (spearman(KL, |δ|) = +0.237 overall, +0.407 among non-governing — the strongest continuous predictor in the eval), and in the hybrid tier's washed-out recurrent state. In every case today's answer is "recompute more"; the bet is that a small adapter buys some of it back, so reuse can be applied where a conservative planner must refuse it.

**Method: stitched-KV consistency fine-tuning** (`src/marathon/stitch_train.py`, designed in [phase3-design.md](phase3-design.md)). LoRA on q/k/v/o, forward passes run *with the stitched cache* (P verbatim, E′ fresh, S re-rotated), loss = KL to the frozen base model's full-recompute next-token distribution over the continuation — self-distillation where the teacher is the model's own clean-context behaviour, and the loss is literally `kvshift_eval`'s reported `klmean`. An anchor term runs the student on a *clean* cache against the same teacher; it is 0 for the base model by construction, so it reads out adapter-induced damage in the same units. Data is `kvshift_eval`'s own builders on fresh seeds, half governing edits and half other kinds for regularisation, with held-out seeds for eval. RoPE re-rotation is a fixed rotation and the scatter is a copy, so stitched attention is differentiable end to end; the reused KV is a constant (nothing can backprop into a previous turn), which is what keeps it cheap — gradients flow only through the fresh span's and the query's forward.

**Exit criteria** (held out; same metric as the 144-session eval): governing/non-governing mean KL ratio ≤ 2×, down from ~9×, with the median ratio (~3–4.5×) no worse, with no held-out governing item over KL 0.2; clean-context KL of the adapted model vs the base ≤ 0.002 (the `prefix-equiv` noise floor) and clean planted-fact accuracy unchanged within one item; no regression on the 144-session eval (non-governing klmean no worse than the seed-to-seed spread, ~20%, and no increase in items over KL 0.05); `kvshift_probe --scenario dep-instruction` first-token KL down from 0.3492 toward the `first-512` level at reuse-all's 0.4% recompute; and no change in tokens forwarded, since the adapter changes weights, not the reuse plan. Failing the clean-context or regression gate is a failure regardless of the governing result — efficiency that changes answers is a regression, not a win. Payoff if met: `reuse_plan` downgrades governing edits from `repair` to `reuse`, i.e. 1.5% instead of 68% of tokens forwarded on exactly the edit that invalidates the most history.

**2026-08-20 (17:30) — the mixed retrain died in the backward, inside the OOM handler written for it.** The constructive half of the paged experiment — does training *on* the paged population repair it? — did not run. `scripts/stitch_mixed_retrain.sh` (100 paged + 100 synthetic, w=2, grad-prefill cap 6000) died before item 21 with WSL's `device not ready`, at `example_losses -> Tensor.backward`. The fallback added earlier that day wrapped only the **forward**; the backward is the memory peak, so the handler was one frame too high. Fixed: forward+loss+backward are now one retryable unit, and because a part-way backward has already accumulated gradient, the adapter's grads are snapshotted and restored before the retry (a naive retry double-counts, silently, only on OOM items) — with a test that seeds a known gradient, makes `Tensor.backward` accumulate then raise, and asserts the result matches a clean cheap-path item. Nothing survived the crash (adapter and JSONL are written at the end; the first checkpoint at 50 was never reached). **Tomorrow, in order: measure peak memory on ~20 mixed items before committing an hour, then train on the paged population and eval under the powered protocol** — and separately the free-running decode on the paged population, which is what decides whether stale attention or the connector explains serving's wrongness. See [findings.md](findings.md).

**2026-08-20 (late) — the paged population: the adapter transfers *nothing* to the workload that matters, and the failure there is 9x worse.** Track L's "reuse turns are wrong 75-91%" and this phase's "the adapter works on governing edits" could not both describe one system, so the population was rebuilt to Track L's shape: `marathon.paged_eval` composes `cold_eval.build_session`, `cold.ColdTier` (real policy, real `[cold #k hash8: ...]` stubs) and `kvshift.token_segments` into **multi-segment** paging transitions — a front demotion plus up to two promotions — with the question asking for a fact behind the churn. Measured shape: 6.4 reuse segments/item, 92% of the view reused, ~5.6k tokens. `Example.span` now takes an explicit `list[Segment]`, since `token_span` collapses disjoint edits and throws the reuse away. **Base failure: median klmean 0.0186 against the synthetic population's 0.0021 non-governing / 0.0071 governing, 18% of items over 0.05 against ~1% — the whole distribution has moved, not just a tail**, which is the shape Track L's numbers imply. **The w=2 adapter's paired delta here is -0.00013, CI [-0.00083, +0.00058], 220/422 improved — a coin flip.** It transfers zero, which retires the morning's "qualified yes": the mechanism is real on the population it was trained on and irrelevant on the one that ships. **But `planted-fact ok` is 498/500 for reference, base and tuned alike**, while Track L sees fact EM at 7/14 — so this reproduces the *distributional* failure and not the *answer-level* one, and stale attention is **necessary but not sufficient** to explain serving's wrongness (teacher-forced 32 tokens on 8B HF here vs free-running generation on 14B-FP8 through the connector's store/staleness/chunked-prefill machinery there). Next discriminating experiment: a free-running decode on this population. Also fixed before it cost a run: `example_losses` selected the do-no-harm hinge for anything not `"governing"`, so every paged item would have trained with a zero gradient — `TARGET_KINDS`/`is_target` now name the distinction and the checkpoint rule buckets on it. Pre-registered: **the paged population is the primary one**; a gate passed only on synthetic edits must be described as such. See [findings.md](findings.md) and [phase3-design.md](phase3-design.md).

**2026-08-20 (afternoon) — the powered gate run: the effect is real, it halves the >0.2 tail, and two criteria still fail.** Eval-only, no training, against the existing `w=2` checkpoint-150 adapter: **1400 items over two clean seeds, 1123 reference-stable, 502 stable governing / 525 non-governing** — both gated buckets past the pre-registered 400. Governing paired mean delta **−0.00775, 95% CI [−0.01038, −0.00537]**, median −0.00025, trim20 −0.00059, sign test **285/502** (p≈0.002); removing the largest improver moves the mean 6%, not 99%. **This supersedes the morning entry's "the effect is one session"** — that was a small-sample artefact, and a statistic too weak to detect an effect is also too weak to rule one out. Criteria: **1a pass** (superiority), **1b fail** (>0.2: 26→13, 13 fixed, **0 newly broken** — the best tail result of the phase; but >0.05: 49→41 with **6 newly broken**, and the rule says none may appear), **1c pass**, **2 fail** (clean drift 0.00201, CI [0.00186, 0.00218] — iteration 3's 0.001999 "pass" was a small-sample accident; damage concentrates in the `standing-governing` bucket at 0.00334), **3 pass** (non-inferiority: CI upper +0.00054 < tolerance 0.00097), **5 pass**. Shape matters more than the verdict: mean −0.0078 against median −0.00025 means **a tail repair, not a distribution shift** — nothing for the typical edit, a lot for the worst, which is the direction that matters since the tail is why governing edits get recomputed. Reference stability measured for the first time: **80% stable, unstable carry 23% of base KL** (the morning's 74%/47% proxy was wrong, the second badly). Given Track L's 75–91% wrongness on the real cold-tier workload, the verdict on the load-bearing bet is a **qualified yes** — the mechanism is real, trainable, and halves the worst tail — with two limits: the absolute effect is small, and **this population is not Track L's** (synthetic single-span edits at 4–8k vs paged, stub-heavy, multi-reuse-turn composition). **Next is not another sweep on this population — it is rebuilding the eval around Track L's failing turns and re-running this protocol on it**, plus a retrain with `--mid-eval-seed` off the eval seed, since the checkpoint under test was selected on a slice drawn from the old eval seed. See [findings.md](findings.md).

**2026-08-20 — the measurement was the result, and iterations 1–3 are "not measured" rather than partial successes.** Chasing iteration 3's irreproducibility finding changed the phase's conclusion. The variation is **path dependence, not noise**: the base columns of the `w=2` and `w=4` evals agree on 120/120 items bit for bit, while the `--base-only` pre-check disagrees on 33/120, because loading an adapter changes the allocation pattern, which changes cuBLAS kernel selection, which in bf16 flips near-tie argmaxes and with them the whole teacher-forced continuation. So **averaging k identical passes buys nothing** — only a deliberate perturbation probe is informative. Applying a paired per-item delta (one item, one pass, one teacher) to reference-stable items, **the governing effect disappears**: `w=2` mean delta −0.0051, 95% CI **[−0.0159, +0.0009]**, median +0.00008, sign test **16/34**; the sum of all 34 deltas is −0.1732 of which **−0.1723 is a single session**, which was over KL 0.2 before and still is. Zero items crossed 0.05 or 0.2 in either direction. Retrospective power at the n every gate run has used: **18%**. The gates are rewritten and pre-registered in [phase3-design.md](phase3-design.md): paired per-item deltas with a 10k-resample bootstrap CI as the gated statistic (ratio-of-means demoted to descriptive), reference-unstable items excluded and reported, mean/median/trimmed always together, base provenance fixed to the same pass, clean-drift gated on the **CI upper bound** (w=2 "passed" at 0.001999), no-regression restated as an explicit **non-inferiority** test, `dep-instruction` demoted from gate to diagnostic, and **≥400 reference-stable items per bucket** (~1,100 eval sessions, ~9× current) since power is 18% at n=46 and 87% only by n=200. Also fixed: `--base-only` was accepted but never forwarded to `evaluate`. **Next GPU run is eval-only** at n≈1,100 against the existing `w=2` adapter under the new protocol — the cheapest experiment that turns three ambiguous results into one answer, and it needs no training. See [findings.md](findings.md).

**Iteration 3 (2026-08-19): re-run at 4–8k, and the gates fail on both hinge weights — iteration 2's promising ratios were mostly a regime artefact.** Pre-registered before the run (regime, hinge-weight ordering `w=2` then `w=4`, checkpoint-selection rule, new bucket). On the base at 4–8k the failure is **5.26× mean / 2.11× median** (n=120), against 3.74× at 4–6k — so iteration 2 was measuring an easier problem. With `--grad-prefill` + the hinge, the best arm (`w=2`, checkpoint chosen by rule) reaches **3.98× mean / 2.26× median**, the two items over KL 0.2 survive, and `w=4` is worse on the tail (p95 0.0328 → 0.0504, a third item over 0.05). Criterion 3 passes on both statistics for the first time (+8.9% mean / −6.6% median) and criterion 2 passes by 1e-6 (0.001999); criteria 1 and 4 fail in both arms. **Two results matter more than the verdict.** (i) *The measurement is not reproducible*: the same base on the same 120 items gives 87/120 bit-identical rows and 33 that differ, worst case 0.1351 vs 0.4910 on one item, because bf16 non-determinism flips the teacher's greedy token and the whole forced sequence changes — so cross-run mean-ratio comparisons at this n are inside the noise, which retroactively weakens every cross-iteration ratio claim here. The in-run paired base/tuned comparison is unaffected. (ii) *The `dep-instruction` distribution gap was not the explanation*: a new probe-shaped `standing-governing` bucket in training and eval barely exhibits the failure (base mean 0.0061 vs core governing's 0.0284) and criterion 4 still fails, so the probe is measuring something the synthetic population does not contain. Confound to carry forward: a WSL OOM forced the `--grad-prefill` cap to 6000, so only **78/200** training items got the expressive path (vs 200/200 in iteration 2) — this is a 39%-strength arm B at 4–8k, not arm B at 4–8k. **Next is measurement, not hyperparameters**: paired per-item deltas with a bootstrap CI instead of a ratio of means, more seeds for the base, and a decision on whether `klmean` over a teacher-forced sequence whose first token can flip is the right target at all. See [findings.md](findings.md).

**Iteration 2 (2026-08-19): the collateral damage is fixed and the tail is gone; the ratio gate still misses on the median.** Two arms at 4-6k, n=120 held out, same base and items for both: a **do-no-harm hinge** on non-governing items (`relu(klmean_student - klmean_base)`, zero while the adapter is at least as good as the base) cut the regression from iteration 1's +37% mean to **+13%** (arm A) / +17% (arm B), and clean drift to **0.0019** in arm A, which clears the 0.002 gate for the first time. **Both arms eliminate every item over KL 0.05** (base 3, worst 0.1480 -> arm A max 0.0489, arm B max 0.0281), which is the number `reuse_plan` actually cares about. Enabling `--grad-prefill` (arm B, so the model also learns what the fresh span *writes* into the cache) is the stronger learner by ~2x throughout the checkpoint curve and reaches a **1.56x mean ratio**, the only run yet to clear the <=2x mean bar; arm A is better on clean drift and the non-governing median. Neither passes all gates: both miss on the median ratio and on `dep-instruction`, which got *worse* in both arms - a distribution gap, since the probe's 20-turn sessions are unlike the 4-6k training data. Mid-training checkpoints also settled iteration 1's ambiguity: the held-out curve is non-monotone but genuinely downward, so the improvement does track training. Caveat that matters: at 4-6k the *base* failure is only 3.74x mean (vs 7.25x at 4-8k), so the gate is easier here and the winner must be re-run at 4-8k. See [findings.md](findings.md).

**8B iteration 1 (2026-08-19): the mechanism is real and the gates are not met.** On 60 held-out sessions (Qwen3-8B, 4-8k tokens, LoRA r=16 on q/k/v/o, 200 training items, lr 3e-5) the adapter cuts governing-edit KL by **43% on the mean (0.0372 -> 0.0211) and 61% at p95 (0.2344 -> 0.0905)**, halving both the items over KL 0.05 (4 -> 2) and those over 0.2 (2 -> 1), with the two worst items in the run improving substantially. The governing/non-governing mean ratio falls 7.25x -> 3.01x. But non-governing edits get 37% worse (0.0051 -> 0.0070), clean-context drift is 0.0024 against a 0.002 budget, and one item still sits at 0.278 - so criteria 1, 2 and 3 fail while 4 and 5 pass. Verdict: **promising, not shippable** - the tail on the one class that forces full recompute is trainable, but not yet without taxing the 191/269 cases the governing flag deliberately leaves alone. Notably this used only the cheap half of the method (`--grad-prefill` off, so the model never learned what to *write* into `E'`), which is the first thing to try next, along with an explicit stitched-KV regularisation term on non-governing items. See [findings.md](findings.md).

**Earlier pilot 2026-08-19 (Qwen3-0.6B): negative, and the negative was about the testbed.** The held-out population did not reproduce the failure class — governing/non-governing mean KL ratio 1.50× against the 8B eval's ~9×, zero of 120 items over KL 0.05, `dep-instruction` at klmean 0.0038 instead of breaking — so the adapter had no signal to exploit: stitched KL 0.0028 → 0.0033 (no improvement) for 0.0031 of clean-context drift, failing the clean-context and no-regression gates. Causes: 0.6B follows standing instructions too weakly to be disturbed by a stale one, 3–5k sessions instead of 4–8k, and a harness defect that asked only the `fact-at` query. The instrument itself checks out (differentiable stitch identical to the serving path, gradients reaching the adapter, anchor exactly 0 at identity). Next: Qwen3-8B at 4–8k with the full query pool, base-only eval first to confirm the ~9× before training. See [findings.md](findings.md).

**Still deferred from the original text:** red-teaming baseline poisoning. A trusted substrate is a high-value target whether the trust is a training objective or a cache lookup, and the KV path arguably makes it worse — poisoned KV is not even inspectable as text. That belongs with the cross-cutting integrity work below.

## Cross-cutting: integrity and operations (never deferred)

Content addressing and hash-chain verification shipped in Phase 0 by design — a trusted substrate is a high-value target, and the model will treat poisoned baselines as settled truth. Single-writer-per-session concurrency and store eviction landed in the KV layer on 2026-08-19: the shift connector's store is keyed by session id with a token budget and LRU eviction across sessions, one in-flight writer per session is enforced rather than assumed, and the scheduler side declines a load whose source positions were evicted (a miss is a recompute, never a wrong answer) — see the "Connector" section of [protocol.md](protocol.md) for what that does *not* yet cover (tensor parallelism, preemption, chunked prefill interleaved with a load). Still ahead: signed snapshots, the same TTL/eviction story for the *baseline* store rather than the KV store, and observability (per-turn metrics exported, not just printed). Every phase keeps the same regression rule: efficiency that changes answers is a regression, not a win.

## North-star metrics

Per-turn prefill tokens → O(|diff| + |input|). TTFT flat as session length grows. Correctness vs full-context replay within tolerance. Cost per turn flat as session length grows.

@acrosley 2026-08-17
